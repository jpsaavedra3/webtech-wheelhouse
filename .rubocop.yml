# Omakase Ruby styling for Rails
inherit_gem: { rubocop-rails-omakase: rubocop.yml }

# Migrations are not edited once they are committed, so they are not linted either.
AllCops:
  Exclude:
    - "db/migrate/**/*"